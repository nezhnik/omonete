# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# конфиг крона с паролем БД — не коммитить
scripts/cron-metal-prices-config.php
scripts/.cbr-usd-cache.json

# временный файл скрипта favicon
.favicon-temp.png

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# резервная копия экспорта монет (можно убрать из .gitignore, если нужно закоммитить снимок)
/backup-coins-data

# экспорт статей для импорта в БД (генерируется из /api/mint-articles-export)
mint-articles.json
